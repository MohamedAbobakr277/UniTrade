## 2024-04-25 - Adding Tooltip and aria-label to IconButtons
**Learning:** Icon-only buttons (like Material UI's `<IconButton>`) without `aria-label` attributes are inaccessible to screen readers. Adding a tooltip provides context for sighted users, and `aria-label` provides context for screen readers.
**Action:** Use Tooltip and aria-label for all IconButtons.
